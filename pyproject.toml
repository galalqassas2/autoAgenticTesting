[tool.ruff]
line-length = 200
exclude = ["apps"]
[tool.ruff.lint]
select = ["E", "F", "W", "I"]
# Ignore E402 (Module level import not at top of file)
ignore = ["E402"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["src"]
norecursedirs = ["apps", "node_modules", "dist"]
